name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Get yadm and install the files
      run: |
        sudo apt-get install yadm
        yadm clone --botstrap https://github.com/$GITHUB_REPOSITORY
        nvim '+checkhealth'
